LRS - Lowest cost option with basic protection against server rack and drive failures. Recommended for non-critical scenarios.
Geo-Redundant storage - Intermediate option with failover capabilities in a secondary region Recommended for backup scenarios.
Zone-redundant storage -Intermediate option with protection against data center-level failures. Recommended for high availability scenarios.
Geo-zone-redundant storage - Optimal data protection solution that includes the offerings of both GRS and ZRS. Recommended for critical data scenarios.

Scenario 1
==========
Your company currently stores large volumes of structured and semi structured data in an on-premises data warehouses and file servers. Leadership has decided to migrate this data to Azure Data Lake STorage Gen2 to enable better scalability, analytics and integration with other Azure services.
How would you approach migrating data from the on-premise systems to Azure Data Lake Storage Gen2?
Integration Runtime - Is a compute or a resource which is required to migrate your data. Which provides the necessary computation resources to migrate your data and perform all the data flows to perform all the data transformations. Basically the activities within the Azure Data Factory.

autoResolveIntegrationRuntime - The best integration Runtime to perform all the operations within Azure network.
When you want to pull your data form OnPremise to Azure cloud you need to create SELF HOSTED INTEGRATTION RUNTIME, which will be managed by me. Pefofrm data flows, data movement and dispatch activities to external compute.

Azure-SSIS - Lieft and shift existing SSIS pacjages to execute in Azure. Before Cloud Azure SSIS was the primary source is the integration between your SQL servers with the external provders now we use ADF.
Airflow - Use this for running your existsing DAG's

Once you create the self-hosted Integration Runtime we need to install the package on our local machine and then we need to paste that particular code which it provides to us and then connection will be successful.

Follow up question - STeps are fine but in real life i as a developer will be using the company laptop now if we want to transfer some data from your machine from your laptop hoe other developers can do that? 
In real world we wont be using our machines. We will be using something called as virtual machines/servers and we create those virtual machines/virtual servers as our itegration runtime machines. So instead of downloading the integration runtime on local we will be doing same on virtual machines and this mahcine will behave like a integration runtime once you provide the key.

When you see throtling like CPU is >90% ..? - You can scale up your virtual machines so that your mahcine can handle the load.
Once you create IRS -> you can pic your destination and select your SELF HOSTED SSIR.

SCenario 2
==========
Your company receives daily exchange rate data from third-party financial service through a REST API. This API returns data in JSON format and requires an authentication token that is refreshed every 60 seconds.
How would you design and implement ths solution using Azure Data Factory ? Walk through the key components and steps you would use.
In ADF -> create pipeline -> select WEB activity / alternate to POSTMAN -> give your URL -> and get the response -> to store the data somewhere send the output via COPY ACTIVITY and select the input as REST with its LS and the sink with its LS.
  Incase of json data under the mapping it wont select the main key and we need to include as it treats as different entity uncheck the boxes and check again to import schema.
Note - if the response is not in the form of JSON, if in csv,  will you use REST API ? No, you should use HTTP CONNECTION , iT IS A recommended approach suggested by Miccrosoft that if we are gettubg the data directly as a response not in the form of JSON , Your always use HTTP.Because it will treat that as a data and dump and bring it to your destination data lake. You can use WEB activity but it needs lots of transformation logic. So it is recommended to use if you are getting data in the form of delimited text directly in the API calls and for web activity you can use and provide the base url but it will be a JSON output  and it requires lot of transformations.
So it is always recommended if you are getting data in the form of delimited text directly in the API calls always use HTTP connection and you can directly decide which format you want to use to ingest the data.


Scenario 3
============
You're working on an ELT pipeline in a cloud-based data platform likeAzure datafactory, AWS Glue, or Databricks. One of the pipeline activities is an API call to fetch data from a third-party service. This API is known o occasionlly fail due to temporary network glitches or rate limiting issues. How would you design this pipeline activity to handle "intermittent failures" gracefully without rerunning the entire pipeline?
This is more on the Networking side, which is not under your control.
In the pipleine under each activity you will be having the retry option which runs only the failed activity instead of rerunning entire pipeline. (bABY don't say i see monitor tab and all, its network issue babe).

Scenario 4
==========
You are working as a Data engineer in a retail company. The company relies heavily on daily ETL pipelines built in Azure Data Factory (ADF) to load sales data from various regions into a central data warehouse. Recently, a few pipeline failures went unnoticed, which caused delays in business reporting and decision-making.
  How would you design and implement a monitoring and alerting mechanism in ADF to notify the team immediately in case of any ETL pipeline failure?
Under Monitor Tab -> Alerts & Metrics -> new alert Rule -> select the criteria -> configure notification -> select your group name -> add email/sms, ..
Before you do this you should enable and register -> it is bit costly to fetch metrics.

Scenario 5
==========
You're working on a PySpark job that processes customer transactions data from multiple sources. The initial steps heavy transformations like joins, filters, and aggregations, and intermediate result is reused in multiple parts of the pipeline - forst for generating KPIs. then for writing different outputs to Delta tables, and finally for some visualizations.
  Given that the same intermediate Dataframe is reused in multiple stages, how would you optimize performance in this situation?.
Cache() - A persist method without parameters  and persist() - with some special properties.
In the pool resource we have storage and execution and in execution all your tasks things happen and if you want to store your df then it gets stored in the STORAGE MEMORY and EXECUTION MEMORY can use that when needed instead of reading from disk.

Scenario 6
==========
You're working on an Azure Data Factory (ADF) pipeline that involves calling a REST API through a WEB activity. This API requires an access key for authentication. To follow the best practices, your team has decided to store the API key securely using AZure key valut instead of hardcoding it in the pipeline.
  How would you implement the above scenario end-to-end in Azure data Factory? walk me through each step you would take, from creating the Key Valut to using its secret in the Web Activity, and ensuring secure handling of the output.
Azure key valut resource -> create -> go to that created resource -> goto objects/secrets -> add the name and value -> create.  -> it can have verisons if you want to edit it.
  Before doing that you should have power to create secrets within key valut -> goto IAM -> add role AZURE KEY VALUT ADMINISTRATion.. -> select member -> add my email -> create secrets now.
  In pipeline - web activity -> add your secret URL?verison -> GET method -Authentication- SYSTEM-ASSIGNED MANAGED IDNETITY, why, because you Azure Data Factory should have the access. -> Add resource, https://vault.azure.net/  -> run, but thsi will display the results so to protect it check the secure output box -> its secure now.
  iN ORDER TO MAKE THE API call, you need to assign the role to this Azure Data Factory as well to read the Key Valut information just like you assigned yourself to read it.
IAM - add role -> KEY VALUT ADMINISTRATor -> Managed Identity(not user group ) -> pick your ADF name  -> ASSIGN.

Scenario 7
==========
The company receives daily sales data files from multiple regional branches. These files are uploaded at random times throughout the day to specific folder in Azure Data LAke storage Gen2 account. Your goal is to ensure that as soon as a new file arrives oin this folder, an Azure Data Factory pipeline is triggered automatically
  How would you design a solution in AAzure data Factory (or Synapsse) to automatically trigger the pipeline whenever a new file is added to the specified folder in Azure Data Lake storage Gen2?
pipeline -> trigger -> select type of trigger  -> these are nore inclined towards scheduling a pipeline and select the storage events trigger and provide your storage details, you can also select the event based on Blob created or Blob deleted.
  WHAT if withput event trigger -> VIA VALIDATION ACTIVITY -> which will continously searches the new files not a better one but via one such.

Scenario 7
===========

Your're working as a data engineer for a retail company that stores large volumes of historical customer transaction data in an Azure Data Lake storage gen2 in CSV format. The business wants to run ad-hoc SQL queries on this data from Azure Synapse Analytics without copying it into the Synapse dedicated SQL pool.
How would you uswe Polybase in Azure Synapse Analytics to query this extended data stored in Data Lake? Please explain steps involved.
Azure Synapse has too pools - 
1. Azure synapse serverless SQL pool - we donot copy the data, we use something calles as Polybase.
2. Synapsededicated pool - we copy the real data inside data warehouse or real tables, very expensive and one reason why tend to serverless SQL pool.
step 1 - Create mastr key for your database
step 2 - Create the credential - access to that particular storage, which will be in this case Synapse Analytics.
step 3 - will be creating external data source.
step 4 - creating external file format.
step 5 - Create external table.
After all these steps you can do adhoc query without copying the data and have the data in datalake itself.

Scenario 8
==========
You are responsible for designing a synapse pipeline to incrementally load data from the Azure SQL source into Data LAke using Parquet format, without loading the entire dataset each time. The company also wants the solution to be efficient and cost-effective. How would you implement incremental loading in Synapse pipelines in this scenario?

  1. Water marking approach. - for other source including SQL database.
under copy activity you do select * with table WHERE update_date > x and that where clause is your date or important column
  2. That x will be coming from the lookup activity which tells you the last date. (last load of the destination)
  3. One more lookup which gives you max date of the current data source 
  4. pass 2 and 3 to 1 copy activity and store that in stored procedure. or
  5. You can use COPY activity instead of stored procedure and this copy activit will store your max state and you will not query the stored procedure or table , you will query the  file -> Modern apaproach.

  2. CDC (Change data capture) - This feature is very handy when you have the SQL data database as a source.
UnDER THE ADF - select CDC - source type -
Enable the CDC in the DB
Enable the CDC for table
Connect that table under CDC section you will able to see it.

Scenario 9
==========
You're working on a data transformation job in Spark where large datasets from different sources are being joined and aggregated. The data volumne varies significantly based on the day and data source. Initially, you tuned the join strategy and number of partitions manually, but you observed that jonb still fails sometimes due to skewed data or inefficient joins. You enables broadcast joins, but its hard to predict right size threshold.
  How can Adaptive Query Execution help in this scenario? What specific features AQE would address that issues mentioned, and how would you enable them in Spark?
All the issues can be addresed by AQE from Spark 3.0 version onwards.
Features
1. It calculates the Query stats during run time.
2. It addresses Skewness - it divides the skewed data into multiplt partitions.
3. Dynamically coalesing the partitions.
4. Dynamic picking of the joins.

Scenario 10
============
You are working on an Aazure data factory pipeline that triggers when a new file is uploaded to a DatA Lake container. Each file needs to go through a different transformation activity. You are required to design the pipeline such that it automatically detects the file name and executes the appropriate activity, without running all activities for every file.   
  How would you design this pipeline to ensure that only the correct activity runs for each specific file, based on its name?
GET META DATA ACTIVITY -> select your dataset source -> under field list select child list -> feed the output to FOr loop
FOR EACH ACTIVITY - items -> select DYNAMIC @activity('Get File Names').output.childnames -> inside the forloop select SWITCH ACTIVITY
SWICTH ACTIVITY - it acts like a CASE statement -> Under EXPRESSION -> @replace(item().name, '.','')
Under CASE add the dataset name , enter the tensil and add your required activity or waiut activity.

Scenario 11
=============
You are expected to build a dynamic solution that reads the schema and maps source columns to destination columns automatically at runtime. How do you implement dynamic column mapping in Synapse Pipelines to handle this scenario? Explain your approach detail including
COPY ACTIVITY -> add source -> add sink with location
Under Mapping, import schema you can do what changes you want to..

But the schema should be strict but by dynamic approach.
How ? - under the copy activity code -> get the code for mapping with JSON TRANSLATOR. -> copy 
Delete the mappings under the mapping section -> ADD DYNAMIC CONTENT, 1. Add the @json('{}') 
2. Create a param outside activity , type as object, add your same json value adn now go to the activity -> mapping -> add dynamic content -> @pipeline().parameters.p_mapping.
you can create multiple params and you can create array and loop and make the dynamic content or mapping.

Scenario 12
===========
Yor need to process 10GB of data using Spark. How many Executors you would need, and how much memory you would need for each Executor to get the maximum parallelism? Also, how many cores should be there?
data - 10GB
default partition size = 128MB
10GB/128MB - 80 partitions in total
for max parallelism we need 80 Cores
Ideally you should pic 5 CORES per-executor so that no issues with the hdfs throughput.
Total number of executores = 80/5 = 16 Executors.

5 cores * 128 MB(as per partition) = 640MB, so each executor needs 640MB to process the data/to execute the task.
We need 640 MB to process data and ideally like 3-4 times 640 * 3.5/4 = 2.64GB 
So if the executor has ~2.64GB of memory then only it can process 640MB of your data.
and explain why also because you have spark pool memory which is 60% of the storage execution memory and where 50% of it is for the storage dfor caching and 50% for execution.

Databricks 
==========
Your company is moving multiple datasets from Azure Sources (Azure SQL Database, Azure Blob Storage, and Azure Data Lake STorage Gen2) into Databricks for analytics and machine leaning. The leadership team is concerned about data security lineage and compilance.
How would you design a data governance architecture to ensure that data ingestion from Azure to Databricks is secure, auditable and compliant.
Step 1 - Read the data, in order to read the data, need to use UNITY CATALOG and you need to create ACCESS CONNECTOR which is avilable in AZURE, Which will have READ/WRITE permissions to the DB.
Step 2 - IN Databricks create UNITY METASTORE, while creating the UNITY METASTORE we provide the default storage account to the metastore, which will have the DATALAKE attached to it.
Step 3 - Now create a EXTERNAL LOCATION - A URL pointing to the perticular location. While creating the external location you pass the ACCESS CONNECTOR.
  I think under CREDENTIALS is nothing but your ACCESS CONNECTOR.
Benifit - With the help of UNITY CATALOG, you can perform all the lineage like source of the tables, Auditing, Security, Acess Control etc ....
NOte - SETTING -> DEVELOPER SETTING -> select tabs for notebooks and files, lakeflow pipeline editor.

Your team stores daily transactions files in a Databricks Volume. Each file contains new and updated tyransactions. You've been asked to design a solution that incrementally reads only new data without reprocessing historical data.
  How would you implement this using streaming tables in Databricks?
Note - Streaming tables are build on top of Spark Streaming but a design is bit different.
Question - how can i setup is it normal tables?, Delta live tables or LAkeflow declarative pipelines. What approach?.
CATALOG -> create schema -> create volume -> upload your data for testing.
or CREATE VOLUME catalog.schema.folder
Approach1 - normal way
=======================
df = spark.read.format("csv") \
      .option('header', 'true') \
      .option('inferscheam', 'true') \
      .load('/Volume/<catalog>/<schema>/<folder>')

df = spark.readStream.format("cloudFiles") \
      .option("cloudFiles.format", "csv")\
      .option("cloudFiles.schemaLocation", "/Volumes/<catalog>/<schema>/sink_orders/checkpoint")\
      .option("cloudFiles.schemaEvolutionMode", "rescue")\     #if you use the addnew colum then they might break the downstream schema or downstream flow.
      .option("cloudFiles.maxFilesPerTrigger", "1")
      .load("/Volumes/dbinterview/source/source_orders")


df.writeStream.format("delta")\
  .option("checkpointLocation", "/Volumes/<catalog>/<schema>/sink_orders/checkpoint")\
  .trigger(once=True)
  .start("/Volumes/dbinterview/source/sink_orders/data")\

Jobs and Pipelines 
==================
Ingestion Pipeline, ETL pipeline, Job

Approch 2
==========
import dlt

@dlt.table(
    name = 'inc_table'
)
def inc_table():
    df = spark.readStream.format("cloudFiles")\ 
         .option("cloudFiles.format", "csv")\
         .load("/Volumes/dbinterview/source/source_orders")
    return df

Your team stores daily transaction files in a Databricks Volume. Each file contains new and updates transactions. You.ve been asked to design a solution that incrementally reads only the new data without reprocessing historical data.
  How would you design this streaming table in Declarative pipeline So that the constraints are enforced at the time of the table creation ratio than handling them later in transformations?
 
import dlt

# Adding Expectations
rules = {
  "rule1" : "orderID is NOT NULL",
  "rule2" : "Quantity > 0",
  "rule3" : "Price >= 0"
}

@dlt.table(
  name = "inc_table"
)
@dlt.expect_all(rules)   #its like warning , if you want error then @dlt.expect_all_or_fail, if drop malformed then @dlt.expect_all_or_drop

def inc_table(rules):
    df = spark.readStream.format("cloudFiles")\
         .option("cloudFiles.format", "csv")\
         .load("/Volumes/dbinterview/source/source_orders")

How does Dtabricks Serverless Compute differ from traditional clusters, and what are its advantages?
Serverless compute says that you dont have to worry about the compute settings , scaling...

How would you handle scenario where multiple concurrent queries are overwhelming a Databricks SQL Warehouse, leading to performance degradation?  
Databricks recommends a cluster fr every 10 concurrent queries, The maximum number of queries in a queue for all SQL warehouse types is 1000.

What strategies would yu employ to optimize the performance of a Databricks SQL Warehouse handling complex analytical queries?
Optimize command and Z ordering on datalake which helps in data skipping,
If you want to get rid of partitions then use LIQUID CLUSTERING. Dynamically changes based on query.

You have just started a new data engineering project on databricks that involves multiple team members collaborating on notebooks and code. To ensure proper version control and seamless collaboration, you want t initialize the databricks workspace as a Git repository.
  How would you approach setting up a Git integration in Databricks?
Under Workspace -> can find REPOS or cam create outside of it aswell.

Settings -> USER -> Linked accounts -> Add Git Credentials. You can add the git provider as Azuredevops, Github ...
You also have the Databricks Asset Bundels for CICD -> take a look.

You are working with data Lake ingestion pipeline using lakeflow jobs, The requirement is to process multiple incoming files, each having different schemas and data formats. These files need to be routed and written to different target locations in the data lake.
  How would you design and implement a lakeflow job to Dynamically read and sink the incoming files.
dbutils.widgets.text("file", "")
file_name  = dbutils.widgets.get("file")
df = spark.read.format("csv")\  
        .option("header", "true")\
        .option("inferschema", "true")\
        .load(f"/Volumes/catalog/schema/source/source_order/{file_name}.csv")

df.write.format("delta")\
    .mode("overwrite")\
    .save(f"/Volumes/catalog/schema/source/dynamic/{file_name}")

SQL query -> SQL file 
Note - When ever we want to create parameterized queries, create parameterized query in the SQL file .so that you can pass that particular parameter value dynamically.

fileLookup.sql
createfilelookup.sql
CREATE TABLE IF NOT EXISTS catalog.schema.fileLookup
(
  file string,
  type string
)

INSERT INTO catalog.schema.fileLookup
VALUES
('productsOrders', '.csv')
('orders_day1', '.csv')
steps i followed was 
1. Create a sql file with query which has file anme and the type - select * from dbt_proj.default.filelookup;
2. Create a notebook with above code
3. create a new job 
  3.1 create a new sql task add the sql file
  3.2 add the for loop and iterate thorugh it using - {{tasks.filelookup.output.rows}}
  3.3 within that it directs to the activity adn it has the dynamic ingestion code and consumes the input from the lookup activity and the poarameter is given here as KEY : file, value : {{input.file}}

You are working on a proof-of-concept project to explore data stored in a lakehouse architecture. The core data is managed in Databricks, but for quick data exploration and analysis, your team wants to query extra data directly from external tools outside Databricks.
  Explain the concept of lakehouse federation in this contect and describe the benefits and challenges you might face.
Catalog Explorer -> External data -> (External location, Credentials, Connections (Lakehouse Federation), External Metadata)
Create the connection with all the required credentials -> creats a Foreign CataLOG
Diff with Lakehouse Federation and Lakehouse Connect
Lakehouse Connect - To build your real projects . like migrate the data , Building the physical lakehouse.

You are working as a data engineer on a large-scale data platform using Delta Lake. Your team needs to create multiple copies of a large Delta table to run different data experiments and transformations in parallel, but you want to avaoid duplicating the actual data files to save storage.
  How would you approach this requiremnet using Delta Lake features?
Clonning techniques in Delta lake babe
Deep Clone - Is a clone that copies the source table data to the clone target in addition to the metadata of the existing table. additionally, stream metadata is also cloned such that a stream that writes to the Delta Table can be stopped on a source table and continued on the target of a clone from where it left off.
A shallow clone - Is the clone that does not copy the data files to the clone target. The table metadata is euivalent to the source. These clones are cheaper to create. And supported for managed table.
  Dependency - ANy chnage sto the source definitely effects the cloned ones.
CREATE TABLE catalog.schema.table SHALLOW CLONE <clone source>

Imagin eyou are working as a dataengineer for a company for a company that has multiple business units, each with its own Unity Matastore in databricks. These units need to securely and efficiently share subsets of their data with each other without duplicating data.
    How would you design a solution to enable seamless data sharing across these multiple Unity Metastore
I Unity catalog you annot directly share data with other unity metastore.
DElta sharing - Which allows to share the data between two different meta store.
1. Databricks Open Source Delta Sharing.
2. Databricks to Databricks  Delta Sharing** - you can create recipient
Since you can query from other metastore this is often called Foreign Catalog.

Imagine you are working fora financial services company that stores sensitive customer information personally identifiable information (PII) suh as social security numbers, credit card details, and addresses. Your data engineering team needs to provde data access to the analytics team for reporting urposes, but they must ensure that sensitive PII data is protected and not exposed in the reports.
    How would you approach securing the PII data before sharing it wih the analytics team?

This is the concept of dynamic data masking, we can apply dynamic data masking on the report side aswell, but here it is focused on the lakehpuse level or warehouse level.
We will create the groups like AD groups, Active Directory groups. 
Settings -> Identity and access -> GROUPS -> create your group and add members.
UNITY CATALOG FUNCTION
CREATE FUNCTION pii_masking(p_address String)
  RETURN CASE WHEN is_account_group_member('devAdmins') THEN p_address ELSE '*** **** ****' END;

%sql
ALTER TABLE dbinterview.source.customers
ALTER COLUMN address SET MASK pii_masking;

You are working as a data engineer for an e-commerce ompany. Your team wants to monitor key business metrics in near real-time using Dtabricks SQL. Specially, you need to send automated alerts when certain conditions are met based oin SQL query results.
  There i a table daily_sales with columns: sale_date, product_id, units_sold, and revenue. Your manager wants to receive an alert if the total revenue for any product on a given day drops below $1000.
SETTINGS -> NOTIFICATIONS -> NOTIFICATION DESTINAION -> ADD DESTINATION (username, type, email)

QUERY Notebook
CREATE TABLE orders (
  id INT,
  p_id INT,
  amount INT
 );

INSERT INTO orders VALUES (1,1,10), (2,1,20), (3,1,100) ....

SELECT SUM(amount) as toal sales FROM orders;

 GOTO -> ALERTS -> start tyoing the condition
SELECT SUM(amount) as total_sales FROM catalog.schemd.orders;
under the condition add the condition and then set the alerts and schedule and NOTIFY.
You can also add cherry on top like table name etc...
just like how you do in ADF like run id, task name, error details...

You are working on a Databricks project where a Delta table is used to store streaming ingestion data. oVER time, you notice that the table has thousands of small files due to frequent micro-batch writes. This has started down queries and inceasing job execution times.
  How would you identify this small file problem, and what steps would you take to optimize the table's performance without interrupting ongoing ingestion?
Since you are not writing to the database and you are writing in the form of files the number of files increases based on the ingestions.
Optimize and Zordering - sort the data within the partitions and overall partition level as well so that it can do data skipping usng column statistics.

OPTIMIZE catalog.schema.source.orders ZORDER BY(id)
counter question - hey lets say the table is not created yet , planning for a bnew table, What strategies we should follow and what is that particular optimizaion technique you should be applying on that particular table.
So go with liquid clustering instead of Zordering and Optimize because it is basically use dfor existing tables.
CREATE TABLE catalog.schema.table
(
  id INT,
  name STRING,
  price DECIMAL(10,2),
  catagory STRING
)
CLUSTER BY AUTO
successssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssss


==============================================
from pyspark.sql.functions import *
from pyspark.sql.types import *
# While ingesting data from an external source, you notice duplicate entries. How would you remove duplicates ad retain only the latest entry based on a timestamp column?
data = [("101", "2023-12-01", 100), ("101", "2023-12-02", 150),("102", "2023-12-01", 200), ("102", "2023-12-02", 250)]
columns = ["product_id", "date", "sales"]
df = spark.createDataFrame(data, columns)
#Casting date col from string to date col
df = df.withColumn('date', col('date').cast(DateType()))
# df = df.withColumn('date', to_date(col('date')))
#Drop duplicates
df = df.orderBy('product_id', 'date', ascending = [1,0]).dropDuplicates(subset=['product_id'])
df.display()

# while processing data from multiple files with inconsistent schemas, you need to merge them into a single DataFrame. How would you handle the inconsistency in PySpark?
df = spark.read.format('parquet')\
    .option('mergeSchema', 'true')\
    .load('<path>')
df.display()

What are the key differneces between Spark and Hadoop MApreduce in terms of performance and scalability.
Hadoop map reduce was the framework that we were using to process our data in parallel. Two components map and reduce and This map is the process in which our data was being distributed among nodes among machines among cluster of machines and reduce is the process in which we were aggregating the data and combining the data and storing the data in the distributed file system. And this map step was actually writing all the intermediate data to the disk for everything transformation everything and data is being stored to disk .and this reduce step should go to disk to collect the data. Spark actually stat processing the data in memory.

But still spark writes data to the disk ? Yes it writes the data to the disk but only when the data is not fitting in the memory and only when we say to write the data to the disk and it is still faster compared to map reduce Map reduce is ment for batch processing but spark is ment for both batch and streaming processing. Map reduce is not good for interactive queries and spark is good for interactive queries.


#You are working with a real-time data pipelines, amd you notice missing values in you streaming data column - Catagory.How would you handle null or missing value in such a scenario?
df_stream = spark.readStream.schema("id INT, value STRING").csv("path/to/stream")
df = df_stream.fillNa({'Catagory': 'N/A'})

#You need to calculate the otal number of actions performed by users in a system. How would you calculate the top 5 most active users based on this information?
data = [("user1", 5), ("user2", 8), ("user3", 2), ("user4", 10), ("user2", 3)]
columns = ["user_id", "actions"]

df = spark.createDataFrame(data, columns)
df = df.withColumn('actions', col('actions').cast(IntegerType()))
df = df.groupBy('user_id').agg(sum('actions').alias('total_actions')).orderBy('total_actions', ascending = False).limit(5)
df.display()


# While processing sales transaction data, you need to identify the most recent transactions for each customer.How would you approach this task?
from pyspark.sql.window import Window
data = [("cust1", "2023-12-01", 100), ("cust2", "2023-12-02", 150),("cust1", "2023-12-03", 200), ("cust2", "2023-12-04", 250)]
columns = ["customer_id", "transaction_date", "sales"]

df = spark.createDataFrame(data, columns)
df = df.withColumn('transaction_date', col('transaction_date').cast(DateType()))
df = df.withColumn('flag', dense_rank().over(Window.partitionBy('customer_id').orderBy(col('transaction_date').desc()))).filter(col('flag') == 1)
df.display()
# Window function apply transforations at row level and give us the right index to where ever you want to go..

from pyspark.sql import *
df = spark.readStream.format('cloudfiles') \
        .option('cloudfiles.format', 'parquet')\
        .option('cloudfiles.schemaLocation', 'abfss://mycontainer@storagenmae.dfs.core.windows.net/autosink/check')

df.writeStream.format('parquet')\
        .option('checkpointLocation', 'abfss://mycontainer@storagenmae.dfs.core.windows.net/autosink/check')\
        .trigger(processingTime = '10 seconds')\
        .start('abfss://mycontainer@storagename.dfs.core.windows.net/autosink/data')

==============================================
Q1 - You have a spark job that needs to process 10GB of data. How would you decide the number of cores and memory per executor for this job? Explain your reasoning.
Reserved Memory - 300 MB
User Memory - 40% (4GB - 300MB)
Executor memory/Spark Pool Memroy/ Unified Mmeory - 60% (6GB - 300 MB)     
      Storage Memory - Caching.
      Execution Memory 
10GB = 80 Partitiones, 128MB for 1 partitions (1GB = 8 partitions)
WE woud need atlest 80 cores.
1 Partition = 1 task = 1core.

Assume 1 Executor has 8 cores, so in total 10 Executores and each can process 1 GB of data.
And since Execution memopry is 30 % 
So atleast we need 3GB of memory per executor, safer 3.5 times based on spark pool memory allocation.

Q2 - You are working as a Data Engineer in a company where daily ingestion jobs write into a Data Lake (Delta/Parquet). Over time, the storage is filled with thousands of samll files, causing slow query performance and high job runtimes.
  How would you tackle the small file problem:
    1. While writing the data into Data Laake
    2. After the data is already written?
OPTIMIZE COMMAND IN THE DELTA LAKE
OPTIMIZE <table_name>
All the files will be coalesce, i think it will be 1GB in size.
** If you want to tackle the problem while writing the data you can use **" OPTIMIZE WRITE " it will coalesce the partition before writing.

df.write.format("delta")\
    .option("optimizeWrite", True)\
    .mode("append")\

Special counter question, why is the need of using OPTIMIZE when we use optimizeWrite while writing ?
  LETS think a pipeine writes to delta lake very often and by the end of the day definitely multiple files and to  toptimize we have to use OPTIMIZE <table_name? , so combination of both is the best.

Q3 - How does Spark's Unified Mmeory Manager allocate memory between execution memory and storage memory in this scenaro?
  How does Spark reallocate memory dynamically when execution needs more memory, and what happens to cached data during this process?
During the memory full in execution memory it cna be moved to the storage memory as well when space available.
When storage is already full and but execution memory needs some memory then execution memory takes precedence and the other blocks in the storage memory gets evicted based on LRU (Least Recent UsED).
If more than the capacity ofthe storage exceeded even after utilizing the storage memory then it will spill to disk.

Q4- Your team receives event data as files landing continously in cloud storage. The source team sometimes adds new columns or changes the schema without prior notice.
You need to process these files in near-real time and load into Delta Tab;e without breaking the pipeline. How would you design this streaming solution to handle schema changes safely? What Spark/Databricks features would you use?
Spark structured Streaming + AUTOLOADER 
Main point tonote is in spark strcutured streaming we has the "checkpoint location" and in Autoloader we have additiobal "Schema Location".
It will automatially manages it , the main purposse of checkpoint is "IDENPOTENCY", which is exactly once.

What is the schema changes form source, that is where schema location comes in? 
  Schema once stored in Schema location, it will be cached as well and provides the info to autoloader, when new schema changes it goes to autoloader thinking that no schema changed but autoloader throws an error with UNKOWNFIELDEXCEPTION , then you dont have to change anything just rerun the code then the cached schema and schema is changed.
Even before the stream throws the error ,Autoloader performs schema inference on the latest micro-batch of the data and updates the schema location with the latest schema by mergng new columns to the end of the schema. The data types of existing columns remain unchanged.
Databricks recommends configuring Autoloader streams with Lakeflow jobs to restart automatically after such schema changes.

It supports multiple modes addNewColumns (default) , Rescue, failOnNewColumns, None
(spark.resdStream.format("cloudFiles")
                        .option("cloudFiles.format", "parquet")
                        .option("cloudFiles.schemaLocation", "<path-to-checkpoint>")
                        .option("cloudFiles.schemaEvolutionMode", "rescue")\
                        .load("<path-to-source-data")
                        .writeStream.format("delta")
                        .option("checkpointLocation", "<path-to-checkpoint>")
                        .start("<path_to_target"))
                        
Q5 - Your team is building a data platform on Databricks.
  Daily ETL jobs run once a day and finish in 20-30 minutes
  Ad-hoc analysis is done by data engineers and analysts throughout the day.
  Cost Optimization and cluster stability are important
How would the architecture differ if you use serverless compute vs All-Purpose Compute for this setup? Which workloads would you place on each and why?
Characteristics of All-clusters
===============================
You manage clusters - starts/stop, sizing, auto-scaling policies.
Separate clusters for different workloads to avoid interference.
Cost model - Pay for compute while clusters are running (even idle)
Stability - Predictable, but requires tning for performance/cost.
Complexity - Highe operational overhead.

Chracterstics of serverless
============================
Unified for all workloads
Databricks manage ompute - No cluster creation/configuration
Unified pool - Same compute handles ETL and ad-hoc
Cost model - Pay only for actual compute usage
Stability - Autmatic scaling, no resource contention issues
Complexity - Lower operational overhead (No cluster tuning)
Daily ETLK jobs (20-30 min, once/day)
serverless - Cost optimization - no idle cluster costs. Pau only for the 20-30 min runime, No cluster management, job runs on-demand, Autoscales for the workload, no resource conflists, handles data volumes spil=kes automatically.
All Purpose - Alternate if needed, Use if you custom cluster configs, cost risk- cluster might run longer than needed, or you pay for idle time if you keeo warm, Management overhead - need to schedule start/stop or use job clusters.
Ad-Hoc ANalysis (ThROUghout the day)
Serverless - instant scaling - analysis get compute imeediately. no waiting for clusters to start, Cost Optimization - Pay only when notebooks/queries are running. Stabikity - No resource contention between usrs, User experience - Seam less for data exploration prototyping.
All purpose - ALternate for heavy workloads - use for long running, compute-intensive analysis, Cost risk - if cluster is always-on , high idle costs during off-hours, management - Need to size fpr peak usage, leading to over-provisioning.

Cost and STability Comparision
===============================
Aspect	Serverless Compute	All-Purpose Compute
Cost for ETL	~$0.50-1.50 per job (20-30 min)	$5-20+ per day (if cluster kept warm)
Cost for Ad-Hoc	Pay per query/notebook session	Pay for entire cluster uptime
Stability	Excellent (auto-managed)	Good (but requires tuning)
Setup Time	0 minutes	15-60 minutes per cluster
Scaling	Automatic	Manual/auto-scaling policies
Best For	Variable, unpredictable workloads	Predictable, steady workloads


Go with Serverless Compute for everything.

ETL jobs: Schedule them as Databricks Jobs on serverless compute. They'll spin up, run for 20-30 min, and shut down — minimal cost.
Ad-hoc analysis: Use serverless notebooks for data exploration. Analysts can start working instantly without cluster waits.
Why this setup? Your workloads are short/bursty (ETL) and variable (ad-hoc), which is serverless's sweet spot. Cost savings will be significant vs managing all-purpose clusters.
When to consider All-Purpose:

If you have very large ETL jobs (>2 hours) that need specific hardware optimizations
If you need GPU instances for ML workloads
If you're in a region where serverless isn't available yet


*************************************************************************************
10 GB data ÷ 128 MB/partition = 80 partitions → need 80 cores total

Assume 10 Executors, each with 8 cores:
  10 × 8 = 80 cores ✓  (1 core handles 1 partition at a time)

Memory per Executor:
  8 cores × 128 MB = 1 GB active data per executor
  Rule of thumb: 3–5× active data = ~3.5 GB Spark pool memory

  ┌─────────────────────────────────┐
  │     Executor Container (6 GB)   │
  ├─────────────────────────────────┤
  │ Reserved Memory        = 300 MB │
  │ User Memory (40%)      = 2.3 GB │  ← UDFs, internal metadata
  │ Spark Pool Memory (60%)= 3.4 GB │
  │   ┌───────────┬───────────┐     │
  │   │ Storage   │ Execution │     │
  │   │ (Cache)   │ (Shuffle) │     │
  │   │   50%     │    50%    │     │
  │   └───────────┴───────────┘     │
  └─────────────────────────────────┘

Final Config:
  --num-executors 10
  --executor-cores 8
  --executor-memory 6g
Executor Memory = (Active Data per Executor × 3.5) / 0.6 + 300MB

While Writing (Prevention)
# Option A: optimizeWrite (Databricks Delta)
df.write.format("delta") \
  .option("optimizeWrite", True) \
  .mode("append") \
  .saveAsTable("my_table")

# Option B: Coalesce/Repartition before write
df.coalesce(10).write.format("parquet").save("/path")

# Option C: maxRecordsPerFile
df.write.option("maxRecordsPerFile", 1000000).save("/path")

After Writing (Compaction)
-- Delta Lake OPTIMIZE command (compacts small files into ~1GB files)
OPTIMIZE my_table;

-- With Z-ORDER for query-specific optimization
OPTIMIZE my_table ZORDER BY (date, region);

BEFORE OPTIMIZE:              AFTER OPTIMIZE:
┌────┐┌────┐┌────┐┌────┐     ┌──────────────────┐
│1MB ││2MB ││0.5M││3MB │ ──► │  ~1GB Compacted   │
└────┘└────┘└────┘└────┘     └──────────────────┘
┌────┐┌────┐┌────┐           ┌──────────────────┐
│4MB ││1MB ││2MB │       ──► │  ~1GB Compacted   │
└────┘└────┘└────┘           └──────────────────┘
  Thousands of small files      Few large files

Q3: Unified Memory Manager — Dynamic Allocation
Spark uses a soft boundary between Execution and Storage memory. Both share the Unified Memory pool (60% of JVM heap minus 300MB reserved)

Scenario 1: NORMAL — Both have enough
┌─────────────────────────────────────┐
│  Execution Memory  │ Storage Memory │
│       50%          │     50%        │
│                    │  (cached DFs)  │
├────────────────────┤────────────────┤
│          Soft Boundary              │
└─────────────────────────────────────┘

Scenario 2: Execution needs MORE → evicts cached data (LRU)
┌─────────────────────────────────────┐
│  Execution Memory ──────►│ Storage  │
│       70%                │   30%    │
│  (borrows from storage)  │ (evicted)│
│                          │  LRU     │
└─────────────────────────────────────┘
  Storage data evicted using Least Recently Used

Scenario 3: Execution STILL not enough → Spill to Disk
┌─────────────────────────────────────┐
│  Execution Memory ──────────────►│S │
│       90%                        │10│
│  Still not enough? SPILL TO DISK │  │
└─────────────────────────────────────┘
         │
         ▼
  ┌──────────────┐
  │  Local Disk  │  (shuffle spill)
  └──────────────┘
Key Rules:

Execution can evict storage (cached data) using LRU
Storage cannot evict execution memory
If execution still isn't enough → spill to disk
Cached DFs getting evicted = jobs slow down (re-computation needed)


Q4: Streaming with Schema Evolution — Autoloader
df = (spark.readStream
      .format("cloudFiles")
      .option("cloudFiles.format", "json")          # or csv, parquet
      .option("cloudFiles.schemaLocation", "/mnt/schema_loc")
      .option("cloudFiles.inferColumnTypes", True)
      .option("cloudFiles.schemaEvolutionMode", "addNewColumns")
      .load("/mnt/source/events/")
)

df.writeStream \
  .format("delta") \
  .option("checkpointLocation", "/mnt/checkpoint/events") \
  .option("mergeSchema", True) \
  .outputMode("append") \
  .toTable("bronze.events")
┌──────────┐    ┌────────────┐    ┌───────────────┐    ┌────────────┐
│  Cloud   │    │            │    │  Schema       │    │  Delta     │
│  Storage │───►│ Autoloader │───►│  Evolution    │───►│  Table     │
│ (Source) │    │            │    │  (mergeSchema)│    │(Destination│
└──────────┘    └─────┬──────┘    └───────────────┘    └────────────┘
                      │
               ┌──────┴──────┐
               │ Checkpoint  │  Tracks which files processed
               │ + Schema    │  Stores inferred/evolved schema
               │   Location  │
               └─────────────┘

Q5: Serverless vs All-Purpose Compute
Aspect	All-Purpose Compute	Serverless Compute
Startup	5-10 min cold start	Seconds
Cost model	Pay while cluster is ON	Pay per query/job
Best for	Ad-hoc exploration, notebooks	Scheduled ETL, short jobs
Idle cost	YES (costly if left running)	NO
Auto-scaling	Manual config	Fully managed
┌─────────────────────────────────────────────────────┐
│                   DATABRICKS WORKSPACE              │
│                                                     │
│  ┌──────────────────┐    ┌────────────────────────┐ │
│  │ Daily ETL Jobs   │    │ Ad-hoc Analysis        │ │
│  │ (20-30 min/day)  │    │ (throughout the day)   │ │
│  │                  │    │                        │ │
│  │  ► SERVERLESS    │    │  ► ALL-PURPOSE CLUSTER │ │
│  │  ► No idle cost  │    │  ► Interactive use     │ │
│  │  ► Fast startup  │    │  ► Shared among team   │ │
│  │  ► Job Compute   │    │  ► Auto-terminate=30m  │ │
│  └──────────────────┘    └────────────────────────┘ │
│                                                     │
│  Cloud Storage: AWS S3 / ADLS Gen2 / GCS            │
└─────────────────────────────────────────────────────┘

ETL Jobs → Serverless (spins up fast, no idle cost, pay only for 20-30 min)
Ad-hoc Analysis → All-Purpose with auto-termination (analysts need interactive notebooks)

Q6: Revenue Drop Alert in Databricks
Approach: Databricks SQL Alert + Webhook/Email

-- Step 1: Create a SQL query that checks revenue drop
WITH daily_revenue AS (
  SELECT date, SUM(revenue) AS total_revenue
  FROM sales_table
  GROUP BY date
),
comparison AS (
  SELECT
    date,
    total_revenue,
    LAG(total_revenue) OVER (ORDER BY date) AS prev_day_revenue,
    ROUND((total_revenue - LAG(total_revenue) OVER (ORDER BY date))
      / LAG(total_revenue) OVER (ORDER BY date) * 100, 2) AS pct_change
  FROM daily_revenue
)
SELECT * FROM comparison
WHERE pct_change < -10
ORDER BY date DESC
LIMIT 1;
┌────────────┐     ┌──────────────┐     ┌────────────┐     ┌──────────┐
│ Sales Delta│────►│ Scheduled    │────►│ Databricks │────►│ Email /  │
│   Table    │     │ SQL Query    │     │ SQL Alert  │     │ Slack    │
│            │     │ (runs every  │     │ (triggers  │     │ Webhook  │
│            │     │  15 min)     │     │ if rows>0) │     │          │
└────────────┘     └──────────────┘     └────────────┘     └──────────┘
Steps in Databricks UI:

Create the SQL query above in Databricks SQL Editor
Go to Alerts → Create Alert → link to the query
Condition: "If query returns rows" → trigger
Notification: Add email destinations or Slack webhook
Schedule the alert to run every 15 or 30 minutes


Q7: Reusable SQL with Widgets (No-Code for Business Users)
Solution: Databricks Widgets (Parameters)
-- Step 1: Create widgets (dropdown/text inputs)
CREATE WIDGET TEXT start_date DEFAULT '2026-01-01';
CREATE WIDGET TEXT end_date DEFAULT '2026-12-31';
CREATE WIDGET DROPDOWN country DEFAULT 'India'
  CHOICES SELECT DISTINCT country FROM sales_table;

-- Step 2: Use widgets in the query
SELECT product, country, SUM(revenue) AS total_revenue
FROM sales_table
WHERE date BETWEEN getArgument('start_date') AND getArgument('end_date')
  AND country = getArgument('country')
GROUP BY product, country
ORDER BY total_revenue DESC;

# PySpark equivalent
dbutils.widgets.text("start_date", "2026-01-01")
dbutils.widgets.text("end_date", "2026-12-31")
dbutils.widgets.dropdown("country", "India", ["India","US","UK"])

start = dbutils.widgets.get("start_date")
end = dbutils.widgets.get("end_date")
country = dbutils.widgets.get("country")

df = spark.sql(f"""
  SELECT product, SUM(revenue) as total
  FROM sales_table
  WHERE date BETWEEN '{start}' AND '{end}' AND country='{country}'
  GROUP BY product
""")

┌───────────────────────────────────────────────────┐
│              DATABRICKS NOTEBOOK                  │
│  ┌─────────────────────────────────────────────┐  │
│  │  [Start Date: 2026-01-01 ▼]                 │  │
│  │  [End Date:   2026-03-31 ▼]                 │  │
│  │  [Country:    India       ▼]                │  │
│  │            [▶ Run All]                      │  │
│  └─────────────────────────────────────────────┘  │
│                                                   │
│  Business users just change dropdowns & click Run │
└───────────────────────────────────────────────────┘

Q8: Unity Catalog Architecture

┌─────────────────────────────────────────────────────────────────┐
│                     UNITY METASTORE                             │
│               (One per region, shared across workspaces)        │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  DEV         │  │  TEST        │  │  PROD        │          │
│  │  Workspace   │  │  Workspace   │  │  Workspace   │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         └──────────────────┼──────────────────┘                 │
│                            ▼                                    │
│  3-Level Namespace:  catalog.schema.table                       │
│                                                                 │
│  ┌─────────────────────────────────────────────────────┐        │
│  │  Catalog: prod_catalog                              │        │
│  │  ├── Schema: bronze                                 │        │
│  │  │   ├── Table: raw_events                          │        │
│  │  │   └── Table: raw_transactions                    │        │
│  │  ├── Schema: silver                                 │        │
│  │  │   ├── Table: cleaned_events                      │        │
│  │  │   └── Table: cleaned_transactions                │        │
│  │  └── Schema: gold                                   │        │
│  │      ├── Table: daily_revenue                       │        │
│  │      └── Function: fx_calc_tax()                    │        │
│  └─────────────────────────────────────────────────────┘        │
│                                                                 │
│  Access Control:                                                │
│  ├── Table-level: GRANT SELECT ON table TO group               │
│  ├── Column-level: Column masking (MASK function)              │
│  ├── Row-level: Row filters (WHERE clause filters)             │
│  └── Audit: All access logged centrally                        │
│                                                                 │
│  Storage:  ADLS Gen2 / S3 (Managed or External locations)      │
└─────────────────────────────────────────────────────────────────┘

Key Components:

Metastore: Top-level container, one per region, attached to workspaces
Catalog: Logical grouping (e.g., dev, prod, analytics)
Schema: Database-level grouping under catalog
Tables/Views/Functions: Actual objects
Storage Credentials + External Locations: Govern cloud storage access
Grants: GRANT SELECT ON catalog.schema.table TO group_name
Lineage: Auto-tracked across notebooks and jobs


Q9: SQL Warehouse Scaling for BI Dashboards
Choose: Serverless SQL Warehouse

Feature	Classic	Pro	Serverless
Startup	~10 min	~10 min	Seconds
Auto-scale	Yes	Yes	Yes (fastest)
Scale-to-zero	No	No	Yes
Cost at night	Paying	Paying	$0
Query queuing	Yes	Better	Instant scaling

BUSINESS HOURS (9 AM - 6 PM):
┌────────────────────────────────────────┐
│  Serverless SQL Warehouse              │
│  ┌──────┐┌──────┐┌──────┐┌──────┐     │
│  │Clust1││Clust2││Clust3││Clust4│     │  ◄── Auto-scales UP
│  └──────┘└──────┘└──────┘└──────┘     │      (hundreds of users)
│  Queries distributed across clusters   │
└────────────────────────────────────────┘

NIGHT (6 PM - 9 AM):
┌────────────────────────────────────────┐
│  Serverless SQL Warehouse              │
│               (idle)                   │  ◄── Scales to ZERO
│            $0 cost                     │      (no users)
└────────────────────────────────────────┘

Serverless auto-scales by adding clusters (not just nodes). Each cluster handles concurrent queries. When load drops, clusters are removed — all the way to zero.

Ques10: Your company has a Databricks Lakehouse with Delta tables for sales, customers,
and transactions.
Business users (non-technical) want to ask questions in plain English like:
 - “What were last quarter’s top-selling products in Canada?”
Users
Solution: Databricks AI/BI Genie Space

┌──────────┐     ┌──────────────────┐     ┌──────────────┐
│ Business │     │  Genie Space     │     │  Delta       │
│  User    │────►│  (AI/BI)         │────►│  Tables      │
│          │     │                  │     │  sales,      │
│ "Top     │     │ NL → SQL         │     │  customers,  │
│ products │     │ Translation      │     │  transactions│
│ in       │     │ (LLM-powered)    │     │              │
│ Canada?" │     └──────────────────┘     └──────────────┘
└──────────┘              │
                          ▼
                 ┌──────────────────┐
                 │ Auto-generated   │
                 │ SQL + Results    │
                 │ + Visualization  │
                 └──────────────────┘

Setup Steps:

Create a Genie Space in Databricks AI/BI
Add the Delta tables (sales, customers, transactions) as data sources
Provide instructions/context (e.g., "revenue = price × quantity", "fiscal year starts April")
Share the Genie Space with business users
Users type plain English → Genie generates SQL → returns results + charts

Q11: Flight Ticket Sales Trend Analysis (PySpark)
from pyspark.sql import functions as F
from pyspark.sql.window import Window

df = spark.table("airline.ticket_sales")

# Daily trend
daily_trend = (df
    .groupBy("sale_date")
    .agg(F.sum("sale_amount").alias("daily_revenue"),
         F.count("ticket_id").alias("ticket_count"))
    .orderBy("sale_date")
)

# Monthly trend
monthly_trend = (df
    .withColumn("month", F.date_trunc("month", "sale_date"))
    .groupBy("month")
    .agg(F.sum("sale_amount").alias("monthly_revenue"),
         F.count("ticket_id").alias("ticket_count"))
    .orderBy("month")
)

# Moving average (7-day) for smoother trend
w = Window.orderBy("sale_date").rowsBetween(-6, 0)
daily_with_ma = daily_trend.withColumn(
    "7_day_moving_avg", F.avg("daily_revenue").over(w)
)

daily_with_ma.display()  # Databricks built-in visualization

Q12: Top 5 Airports by Sales Revenue
from pyspark.sql import functions as F

df = spark.table("airline.ticket_sales")

top_airports = (df
    .groupBy("airport_code", "airport_name")
    .agg(F.sum("sale_amount").alias("total_revenue"))
    .orderBy(F.desc("total_revenue"))
    .limit(5)
)

top_airports.show()

Q13: Running Total of Sales per Flight, Reset Monthly
from pyspark.sql import functions as F
from pyspark.sql.window import Window

df = spark.table("airline.ticket_sales")

df = df.withColumn("month", F.date_trunc("month", "sale_date"))

# Window: partition by flight + month, order by date
w = (Window
     .partitionBy("flight_id", "month")
     .orderBy("sale_date")
     .rowsBetween(Window.unboundedPreceding, Window.currentRow))

result = df.withColumn("running_total", F.sum("sale_amount").over(w))

result.select("flight_id", "sale_date", "month",
              "sale_amount", "running_total").show()
+----------+----------+-------+------------+--------------+
|flight_id |sale_date |month  |sale_amount |running_total |
+----------+----------+-------+------------+--------------+
|FL001     |2026-01-01|2026-01|        500 |          500 |
|FL001     |2026-01-05|2026-01|        300 |          800 |  ◄ accumulates
|FL001     |2026-01-20|2026-01|        200 |         1000 |
|FL001     |2026-02-02|2026-02|        400 |          400 |  ◄ resets!
|FL001     |2026-02-15|2026-02|        600 |         1000 |
+----------+----------+-------+------------+--------------+

Q14: Revenue Buckets — Tag Cities as High/Mid/Low
from pyspark.sql import functions as F

df = spark.table("sales_table")

city_revenue = (df
    .groupBy("city")
    .agg(F.sum("revenue").alias("total_revenue"))
)

tagged = city_revenue.withColumn(
    "tag",
    F.when(F.col("total_revenue") < 1000, "low")
     .when(F.col("total_revenue") <= 20000, "mid")
     .otherwise("high")
)

tagged.show()
+----------+--------------+----+
|city      |total_revenue |tag |
+----------+--------------+----+
|Mumbai    |        25000 |high|
|Pune      |         5000 |mid |
|Nashik    |          800 |low |
+----------+--------------+----+

Q15: Dynamic Aggregation (No Hard-Coding

from pyspark.sql import functions as F

# Configuration — change this anytime without touching logic
agg_config = {
    "sale_amount": ["sum", "avg"],
    "quantity":    ["sum", "count"],
    "discount":   ["max"]
}

# Build aggregation expressions dynamically
agg_funcs = {
    "sum": F.sum, "avg": F.avg, "max": F.max,
    "min": F.min, "count": F.count
}

agg_exprs = []
for col_name, funcs in agg_config.items():
    for func_name in funcs:
        agg_exprs.append(
            agg_funcs[func_name](F.col(col_name)).alias(f"{func_name}_{col_name}")
        )

# Apply
df = spark.table("sales_table")
result = df.groupBy("region").agg(*agg_exprs)
result.show()

Benefit: Just update agg_config dict — no SQL/code rewrite needed.

Q16: Deletion Vectors in Delta Lake
Traditional Delete: Rewrites entire Parquet files even to remove 1 row.

Deletion Vectors: Mark rows as deleted in a separate small file — no data files rewritten.
TRADITIONAL DELETE:                    WITH DELETION VECTORS:

┌──────────┐                          ┌──────────┐  ┌──────────────┐
│ Parquet1 │  ─── rewrite ──►  new    │ Parquet1 │  │ DeletionVec  │
│ 100 rows │       entire file        │ 100 rows │  │              │
│ delete 2 │                          │(untouched│  │ rows: [5,23] │
└──────────┘                          └──────────┘  │ (bitmap)     │
┌──────────┐                          ┌──────────┐  │              │
│ Parquet2 │  ─── rewrite ──►  new    │ Parquet2 │  │ rows: [7]    │
│ 100 rows │       entire file        │(untouched│  └──────────────┘
│ delete 1 │                          └──────────┘
└──────────┘                           
                                      Read time: Parquet files are
Cost: Rewrite GBs of data            read but deleted rows are
      for deleting few rows           SKIPPED using the bitmap
      Very slow & expensive           
                                      PURGE later: OPTIMIZE merges
                                      deletion vectors permanently

How it works under the hood:

DELETE writes a small deletion vector file (bitmap) — not rewriting data
On read, Spark checks the bitmap and skips deleted rows
Periodically, OPTIMIZE or PURGE physically removes deleted rows (compaction)


-- Enable deletion vectors
ALTER TABLE my_table SET TBLPROPERTIES ('delta.enableDeletionVectors' = true);

-- Delete is now fast (writes only deletion vector)
DELETE FROM my_table WHERE user_id = 12345;

-- Later, physically purge
OPTIMIZE my_table;
-- or
REORG TABLE my_table APPLY (PURGE);

Q17: Time Travel — Recover Accidentally Deleted Data

-- Step 1: Check table history to find version before the bad DELETE
DESCRIBE HISTORY my_table;
+-------+----------+---------------------+-----------+
|version|operation  |timestamp            |userName   |
+-------+----------+---------------------+-----------+
|   5   |DELETE     |2026-04-23 10:30:00  |you        |  ◄ BAD delete
|   4   |WRITE     |2026-04-23 09:00:00  |etl_job    |  ◄ GOOD state
|   3   |WRITE     |2026-04-22 09:00:00  |etl_job    |
+-------+----------+---------------------+-----------+

-- Step 2: Query the good version to verify
SELECT * FROM my_table VERSION AS OF 4;
-- or
SELECT * FROM my_table TIMESTAMP AS OF '2026-04-23 09:00:00';

-- Step 3: Restore the table
RESTORE TABLE my_table TO VERSION AS OF 4;
-- or
RESTORE TABLE my_table TO TIMESTAMP AS OF '2026-04-23 09:00:00';

# PySpark equivalent
df = spark.read.format("delta").option("versionAsOf", 4).table("my_table")

# Or restore
from delta.tables import DeltaTable
dt = DeltaTable.forName(spark, "my_table")
dt.restoreToVersion(4)

Key: Delta keeps history for 30 days by default (delta.logRetentionDuration). VACUUM removes old files, so don't VACUUM before restoring.

Q18: Liquid Clustering vs Traditional Partitioning
Problem with Partitioning:

Partitioned by date → but queries now filter on customer_id and region
Must drop + repartition = table rewrite (2-3 TB!)
High cardinality columns → thousands of tiny partitions → small file problem
Liquid Clustering:

No rigid folder structure
Data is incrementally re-clustered on write/OPTIMIZE
Can change clustering keys anytime without rewriting the whole table

TRADITIONAL PARTITIONING:              LIQUID CLUSTERING:
(folder-based, rigid)                  (file-level, flexible)

/table/date=2026-01-01/                /table/
  ├── part-001.parquet (1MB)  SMALL!     ├── file-001.parquet (1GB)
  ├── part-002.parquet (2MB)  SMALL!     │   (clustered by customer_id,
/table/date=2026-01-02/                  │    region - data sorted
  ├── part-001.parquet (1MB)  SMALL!     │    within files)
  ├── ...                                ├── file-002.parquet (1GB)
/table/date=2026-01-03/                  ├── file-003.parquet (1GB)
  ├── ...                                └── ...
  
10,000 partitions                      Data is CLUSTERED not PARTITIONED
= 10,000+ small files                 Optimally sized files (~1GB)
-- Enable Liquid Clustering (new table)
CREATE TABLE my_table (...)
CLUSTER BY (customer_id, region);

-- Change clustering keys (NO table rewrite!)
ALTER TABLE my_table CLUSTER BY (region, product_id);

-- Incrementally apply clustering
OPTIMIZE my_table;

Why Liquid Clustering > Partitioning:

Aspect	Partitioning	Liquid Clustering
Change keys	Full table rewrite	ALTER TABLE — instant
High cardinality	Small files explosion	Handled well
File sizes	Uneven	Optimally sized (~1GB)
Incremental	No	Yes (only new data)
Works with	Low cardinality only	Any column
*****************************************************************************************8





































































=========================================================











































































































































































































































